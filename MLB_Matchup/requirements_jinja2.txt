# Requirements for Jinja2-based MLB lineup image generation
# Install with: pip install -r requirements_jinja2.txt

# Core templating engine
jinja2>=3.1.0

# HTML to image conversion
playwright>=1.40.0

# Note: After installing playwright, you need to install the browser:
# playwright install chromium

# Optional: For better image quality and additional features
# pillow>=10.0.0  # If you want to do additional image processing
# selenium>=4.15.0  # Alternative to Playwright for HTML to image conversion
